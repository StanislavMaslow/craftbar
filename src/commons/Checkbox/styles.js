import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {},

  icon: {
    fontSize: 18,
    opacity: 0.6,
  },

  label: {
    fontSize: 14,
    opacity: 0.6,
    fontFamily: 'OpenSansRegular',
  },
});
