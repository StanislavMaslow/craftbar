import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  posts: {
    marginTop: 50,
    // marginBottom: 20,
  },

  header: {
    fontSize: 18,
    marginLeft: 35,
    lineHeight: 24,
    letterSpacing: 0,
  },
});
